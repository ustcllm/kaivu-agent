# Test Artifacts

This directory stores temporary outputs created by semantic checks, replay checks, and local executor smoke tests.

- `tmp/` contains disposable workspace-local scratch directories.
- These artifacts are not part of the kaivu runtime state.

