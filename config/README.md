# Config

This directory stores human-editable configuration for Kaivu.

Recommended contents:

- Agent role and model configuration.
- Tool permission presets.
- Workspace or project defaults.
- Service/runtime settings that should be versioned.

Do not place secrets here. Use environment variables or local ignored files for API keys and credentials.
